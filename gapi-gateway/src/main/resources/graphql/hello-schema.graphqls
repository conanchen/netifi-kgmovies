extend type Query {
    hello(who: String): String!
    people: [Person!]!
}

extend type Mutation {
    createPerson(firstName: String!, lastName: String!): Person!
}

type Person {
    id: Int!
    firstName: String!
    lastName: String!
    fullName: String!
}